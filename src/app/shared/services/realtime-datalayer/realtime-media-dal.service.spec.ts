/* import { TestBed, inject } from '@angular/core/testing';

import { RealTimeMediaDalService } from './realtime-media-dal.service';

describe('RealTimeMediaDalService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [RealTimeMediaDalService]
    });
  });

  it('should be created', inject([RealTimeMediaDalService], (service: RealTimeMediaDalService) => {
    expect(service).toBeTruthy();
  }));
});
 */
